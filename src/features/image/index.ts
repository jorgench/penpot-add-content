import { coverPhoto } from './coverPhoto/coverPhoto.feature'
import { avatarImage, identifyImage } from './profileImage/profileImage.feature'
import { profilePhoto } from './profilePhoto/profilePhoto.feature'

export const ImageOptions = [coverPhoto, profilePhoto, identifyImage, avatarImage]
